/** @type {import('next').NextConfig} */
const nextConfig = {
    // Раньше тут стоял output: 'export' (чистая статика). Перешли на ISR:
    // Next работает как лёгкий Node-сервер (standalone-сборка для Docker),
    // маркетинговые страницы кэшируются «как статика», а каталог/карточка/архив
    // ревалидируются по таймеру и точечно по событию из админки.
    output: 'standalone',

    // Картинки приходят с того же домена через nginx (/uploads). Исходники
    // загружаются в админке в исходном разрешении (нередко ~4K), а на витрине
    // показываются в блоке 250-350px. Без оптимизации браузер получает
    // оригинал 4K и сам его сжимает на лету — из-за этого спустя пару секунд
    // после отрисовки картинка "зернится" (браузер подменяет её на более
    // грубый мип-уровень, чтобы не держать в памяти гигантский растр).
    // Включаем встроенную оптимизацию Next (сервер standalone это поддерживает
    // и в Docker) — next/image будет отдавать уменьшенную копию под реальный
    // размер показа, проблема уходит и трафик резко падает.
    images: {
        remotePatterns: [
            { protocol: 'https', hostname: '**' },
            { protocol: 'http', hostname: '**' },
        ],
    },
};

export default nextConfig;
